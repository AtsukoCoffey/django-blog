from django.apps import AppConfig


class AboutConfig(AppConfig):
    """
    About app: Renewing app of the most recent information of
    the website author and allows user collaboration requests.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'about'
